{
  "$defs": {
    "RelationType": {
      "enum": [
        "follows",
        "features",
        "member_of",
        "replies_to",
        "other"
      ],
      "title": "RelationType",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Directed connection between canonical entities.",
  "properties": {
    "schema_version": {
      "default": "1.0.0",
      "description": "Canonical schema semantic version",
      "title": "Schema Version",
      "type": "string"
    },
    "src": {
      "title": "Src",
      "type": "string"
    },
    "dst": {
      "title": "Dst",
      "type": "string"
    },
    "type": {
      "$ref": "#/$defs/RelationType"
    },
    "ts": {
      "format": "date-time",
      "title": "Ts",
      "type": "string"
    }
  },
  "required": [
    "src",
    "dst",
    "type"
  ],
  "title": "Relation",
  "type": "object"
}